import Http -- imported as 'Http'
import /components/Bar  -- imported as 'Bar'
import Lib/A as AComps
import Lib/B only { b }
import Lib/C only {
  b as C_b
}
import ./types -- imported as 'types'
import ./helpers as Helpers -- imported as Helpers


enum AsyncLoad<T> {
  .loaded
  .loading
  .success(data: T)
}


type Point = {x: Int, y: Int}


default view Real {
  @location: Point = { x: 0, y: 0 }
  @user2: User = User(first-name: 'C')

  render() =>
    <Hello user1=@user1 user2=@user2 updateUser=updateUser />

  fn updateUser(a: User) =>
    @user = a
}
